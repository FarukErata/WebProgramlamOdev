@model IEnumerable<Web_Proje.TEDARİKÇİ>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tedarikçiAdı)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tedarikçiAdı)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.tedarikçiNo }) |
                @Html.ActionLink("Details", "Details", new { id=item.tedarikçiNo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.tedarikçiNo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
