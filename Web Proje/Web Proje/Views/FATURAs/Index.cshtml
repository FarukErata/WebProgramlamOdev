@model IEnumerable<Web_Proje.FATURA>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.faturaTarihi)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.faturaTarihi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.faturaNo }) |
                @Html.ActionLink("Details", "Details", new { id=item.faturaNo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.faturaNo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
