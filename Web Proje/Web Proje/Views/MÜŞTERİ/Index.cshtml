@model IEnumerable<Web_Proje.MÜŞTERİ>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.müşteriAdı)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.parola)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.müşteriAdı)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.parola)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.müşteriNo }) |
                @Html.ActionLink("Details", "Details", new { id=item.müşteriNo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.müşteriNo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
